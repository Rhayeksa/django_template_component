version: "3"
services:
  app:
    container_name: "django_template_component"
    build: .
    ports:
      - "8000:8000"
    # command: ["python", "./manage.py", "runserver", "0.0.0.0:8000"]
    command: "python manage.py runserver 0.0.0.0:8000"
